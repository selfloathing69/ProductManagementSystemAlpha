<local:BaseView x:Class="ProductManagementSystem.WpfApp.Views.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ProductManagementSystem.WpfApp.Views"
        Title="{Binding Title}"
        Height="350" 
        Width="550"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        Background="{DynamicResource AppBackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#007ACC" Padding="10" CornerRadius="4" Margin="0,0,0,15">
            <TextBlock Text="{Binding Title}" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       Foreground="White"
                       HorizontalAlignment="Center"/>
        </Border>

        <Border Grid.Row="1" Background="{DynamicResource PanelBackgroundBrush}" Padding="10" CornerRadius="4" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock FontSize="14" TextWrapping="Wrap" Foreground="{DynamicResource AppForegroundBrush}">
                    <Run Text="ID: " FontWeight="Bold"/>
                    <Run Text="{Binding ProductId, Mode=OneWay}" FontWeight="Bold" Foreground="#007ACC"/>
                </TextBlock>
                <TextBlock FontSize="14" TextWrapping="Wrap" Margin="0,5,0,0" Foreground="{DynamicResource AppForegroundBrush}">
                    <Run Text="Товар: " FontWeight="Bold"/>
                    <Run Text="{Binding ProductName, Mode=OneWay}" FontWeight="Bold" Foreground="#007ACC"/>
                </TextBlock>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="{DynamicResource PanelBackgroundBrush}" Padding="10" CornerRadius="4" Margin="0,0,0,15">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Ожидаемые изменения:" 
                               FontSize="14" 
                               FontWeight="Bold"
                               Foreground="{DynamicResource AppForegroundBrush}"
                               Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding ChangesText}" 
                               FontSize="13"
                               TextWrapping="Wrap"
                               LineHeight="20"
                               Foreground="{DynamicResource AppForegroundBrush}"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="Да"
                    Command="{Binding ConfirmCommand}"
                    Height="40"
                    FontSize="14"
                    FontWeight="Bold"
                    Background="#4CAF50"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="15,10">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#4CAF50"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45A049"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3D8B40"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button Grid.Column="2"
                    Content="Нет"
                    Command="{Binding CancelCommand}"
                    Height="40"
                    FontSize="14"
                    FontWeight="Bold"
                    Background="#D32F2F"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="15,10">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#D32F2F"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#B71C1C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#8B0000"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</local:BaseView>
